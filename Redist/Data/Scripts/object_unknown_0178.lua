--- Best guess: Performs a simple check or update for an object, possibly a status query or minor interaction, with minimal logic.
function object_unknown_0178(eventid, objectref)
    local var_0000

    if eventid == 1 then
        -- callis 0048, 0 (unmapped)
        var_0000 = display_map()
    end
    return
end