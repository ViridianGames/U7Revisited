--- Best guess: Executes an external function with large parameters, possibly for a rare event or system check.
function object_telescope_0744(eventid, objectref)
    if eventid == 1 then
        -- calli 0050, 2 (unmapped)
        wizard_eye(1000, 10000)
    end
    return
end