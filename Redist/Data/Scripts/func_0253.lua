--- Best guess: Manages a torch, changing its type to 701 (lit torch) when used or examined.
function func_0253(eventid, objectref)
    if eventid == 1 or eventid == 2 then
        unknown_0942H(701, objectref)
    end
end