--- Best guess: Handles generic use of an item, delegating to a specific function for processing.
function object_unknown_0660(eventid, objectref)
    if eventid == 1 then
        utility_ship_0777(objectref)
    end
end