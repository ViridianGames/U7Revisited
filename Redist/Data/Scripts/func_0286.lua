require "U7LuaFuncs"
-- Provides information about gold bar exchange rates.
function func_0286H(eventid, itemref)
    if eventid == 1 then
        say(0, "I believe the current exchange rate is one hundred crowns per bar at the Britannian mint.")
    end
end