require "U7LuaFuncs"
function default(eventid, shaperef, frameref)
    debug_print("This is object " .. shaperef .. ", frame " .. frameref .. " and it is using the default script.")
    debug_print("It needs to be replaced with a real script.")
end