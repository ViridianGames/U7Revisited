--- Checks if an object is contained (in inventory or container)
function func_0025(objectref)
    -- For now, assume doors are never contained
    -- TODO: Implement proper check if needed
    return false
end
