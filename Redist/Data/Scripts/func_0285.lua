require "U7LuaFuncs"
-- Provides information about gold exchange rates.
function func_0285H(eventid, itemref)
    if eventid == 1 then
        say(0, "I believe that the current exchange rate is ten crowns per nugget at the mint in Britian.")
    end
end