--- Func081F / 0x81F: open/unlock a secret door piece by transforming it to shape 845.
--- Shared implementation lives in unlock_door() (utility_unknown_0790.lua).

function utility_unknown_0799(objectref)
    return unlock_door(objectref)
end