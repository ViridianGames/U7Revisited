--- Best guess: Calls an external function to handle item interaction, possibly for a specific item or NPC action.
function utility_unknown_0002(eventid, objectref)
    object_unknown_0806(objectref) --- Guess: Handles item interaction
end