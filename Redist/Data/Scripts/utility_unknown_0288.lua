--- Best guess: Sets an item flag (15), possibly for a dance animation or state change.
function utility_unknown_0288(eventid, objectref)
    set_object_flag(objectref, 15) --- Guess: Sets item flag
end